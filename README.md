# rustop

`rustop` is a terminal-based system monitor written in Rust.  
It displays:

- Per-thread CPU load  
- Combined CPU utilization  
- Memory usage  
- GPU utilization (NVIDIA NVML)  
- GPU memory usage  
- GPU temperature  

All values are shown with live-updating bar graphs like:

